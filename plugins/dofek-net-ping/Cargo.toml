[package]
name = "dofek-net-ping"
version = "0.1.0"
edition = "2024"
description = "Dofek plugin: TCP-connect latency to a remote host"

[[bin]]
name = "dofek-net-ping"
path = "src/main.rs"

[dependencies]
dofek-plugin-protocol = { path = "../../crates/dofek-plugin-protocol", version = "0.1.0" }
serde_json = "1"

# Embeds VERSIONINFO — see build.rs. Host-gated: only fetched on Windows.
[target.'cfg(windows)'.build-dependencies]
winresource = "0.1"
